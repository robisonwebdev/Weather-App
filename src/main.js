import displayLocationData from "./displayLocationData";

function main() {
    const main = document.createElement('div');

    main.setAttribute('id', 'main');

    // main.appendChild(displayLocationData());

    return main;
}

export default main;